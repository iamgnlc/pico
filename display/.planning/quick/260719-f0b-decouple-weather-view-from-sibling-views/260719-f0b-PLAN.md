---
phase: quick-260719-f0b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - views/weather_view.py
  - main.py
  - views/clock_view.py
  - views/system_view.py
  - CLAUDE.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUICK-260719-f0b
must_haves:
  truths:
    - "views/weather_view.py has zero sibling-view imports (no `from views import clock_view, system_view`)"
    - "views/weather_view.py has zero bootstrap import (no `import bootstrap`)"
    - "views/weather_view.py exposes public API: should_refresh(now_ms), set_data(ip, temp, code, is_day), render(oled) — old refresh(oled) is gone"
    - "main.py imports bootstrap once at module level"
    - "main.py defines _refresh_all(oled) which fans out bootstrap.fetch() to weather_view.set_data, clock_view.set_tz_offset, system_view.set_wan_ip, then calls weather_view.render"
    - "main.py's two former weather_view.refresh(oled) call sites (boot fetch + scheduler tick) now call _refresh_all(oled)"
    - "bootstrap.py is byte-identical to pre-refactor state"
    - "views/clock_view.py and views/system_view.py bodies are unchanged except for the two comment-line doc updates"
    - "CLAUDE.md prose reflects main._refresh_all as the composition-root fan-out (surgical patches only)"
    - ".planning/STATE.md 'Decisions' list has a new cross-phase entry documenting this refactor"
  artifacts:
    - path: "views/weather_view.py"
      provides: "Pure state-setter + render module; no cross-view or I/O imports"
      contains: "def set_data(ip, temp, code, is_day):"
    - path: "main.py"
      provides: "Composition root with _refresh_all fan-out"
      contains: "def _refresh_all(oled):"
    - path: "bootstrap.py"
      provides: "Unmodified pure I/O layer"
  key_links:
    - from: "main._refresh_all"
      to: "bootstrap.fetch + weather_view.set_data + clock_view.set_tz_offset + system_view.set_wan_ip + weather_view.render"
      via: "direct function calls inside main.py"
      pattern: "bootstrap\\.fetch\\(\\)|set_data\\(|set_tz_offset\\(|set_wan_ip\\(|weather_view\\.render\\("
    - from: "main.py __main__ block"
      to: "main._refresh_all"
      via: "boot-fetch call site (was line 95) + scheduler tick call site (was line 112)"
      pattern: "_refresh_all\\(oled\\)"
---

<objective>
Decouple `views/weather_view.py` from its sibling views by moving the cross-view setter dispatch (`clock_view.set_tz_offset`, `system_view.set_wan_ip`) and the `bootstrap.fetch()` call out of `weather_view.refresh` and into a new `main._refresh_all(oled)` composition-root helper. `views/weather_view.py` becomes a pure state-setter + render module with no sibling-view or I/O imports; `main.py` becomes the sole owner of the boot + refresh fan-out.

Purpose: eliminate the illegal upward/lateral dependency where `weather_view` (a leaf view) was reaching into two other leaf views. Restoring proper layering makes each view unit-testable in isolation, removes the temptation to grow more cross-view dispatch in `weather_view`, and clarifies that `main.py` is the composition root for the boot + poll orchestration (matching the D-38/D-41 pattern already established for clock and system).

Output: modified `views/weather_view.py`, `main.py`, `views/clock_view.py` (comment-only), `views/system_view.py` (comment-only), patched `CLAUDE.md`, updated `.planning/STATE.md`. `bootstrap.py` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@views/weather_view.py
@main.py
@views/clock_view.py
@views/system_view.py
@bootstrap.py

<interfaces>
<!-- Current shape of the code before this refactor. The executor must reproduce these public surfaces exactly (minus the removed weather_view.refresh) and preserve every behavior noted below. -->

Current `views/weather_view.py` public surface (pre-refactor):
- `should_refresh(now_ms) -> bool` — 60s if `_cache_status != "ok"`, else 600s. KEEP BYTE-IDENTICAL.
- `render(oled) -> None` — renders based on `_cache_status` ("pending" | "no_wifi" | "no_data" | "ok"). KEEP BYTE-IDENTICAL.
- `refresh(oled) -> None` — calls bootstrap.fetch(), updates cache, dispatches to clock_view + system_view, then renders. REMOVE.

New `views/weather_view.py` public surface (post-refactor):
- `should_refresh(now_ms) -> bool` — unchanged.
- `set_data(ip, temp, code, is_day) -> None` — NEW. No `oled` parameter. Pure state-setter. First line MUST stamp `_last_refresh_ms = time.ticks_ms()` (D-31 semantics). Sets `_cache_status` per failure semantics (`ip is None` → "no_wifi"; `temp is None` and ip truthy → "no_data"; both truthy → "ok" and update `_cached_temp`/`_cached_code`/`_cached_is_day`). Does NOT call `render`.
- `render(oled) -> None` — unchanged.

Sibling module setters (called by main._refresh_all, not by weather_view):
- `views/clock_view.py::set_tz_offset(offset)` — line 33. Body unchanged; only doc comment on line 34 updates from "called by weather_view.refresh" to "called by main._refresh_all".
- `views/system_view.py::set_wan_ip(ip)` — line 31. Body unchanged; only doc comment on line 32 updates from "called by weather_view.refresh" to "called by main._refresh_all".

`bootstrap.py::fetch()` signature (unchanged, unmodified):
- Returns `(ip, temp, code, is_day, tz_offset, wan_ip)` — 6-tuple. WiFi fail → all-None. WiFi ok + API fail → `(ip, None, None, None, None, None)`.

`main.py` call sites to migrate:
- Line 88: `weather_view.render(oled)` — KEEP AS-IS (pre-fetch "connecting..." paint; render is unchanged).
- Line 95: `weather_view.refresh(oled)` — REPLACE with `_refresh_all(oled)`.
- Line 96-97: `_draw_page_dots(oled, _current_idx)` + `oled.show()` — KEEP BYTE-IDENTICAL.
- Line 108: `VIEWS[_current_idx].render(oled)` — KEEP AS-IS.
- Line 111-118: scheduler tick refresh block. Line 112 `weather_view.refresh(oled)` → `_refresh_all(oled)`. Lines 113-118 (overlay `VIEWS[_current_idx].render` + dots + show) KEEP BYTE-IDENTICAL.
- Lines 119-124: clock_view tick + sync branches. KEEP BYTE-IDENTICAL.

New helper to add to `main.py` (place after `_draw_page_dots`, before `if __name__ == "__main__"`):
```
def _refresh_all(oled):
    ip, temp, code, is_day, tz_offset, wan_ip = bootstrap.fetch()
    weather_view.set_data(ip, temp, code, is_day)
    clock_view.set_tz_offset(tz_offset)
    system_view.set_wan_ip(wan_ip)
    weather_view.render(oled)
```
Plus new top-level `import bootstrap` alongside the existing sh1107/machine/views/text_render imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor views/weather_view.py — drop cross-view + bootstrap imports; replace refresh(oled) with set_data(ip, temp, code, is_day)</name>
  <files>views/weather_view.py</files>
  <action>Edit `views/weather_view.py` to remove the illegal sibling and I/O imports and convert the orchestrator method into a pure state-setter.

  Remove:
  - Line 2: `import bootstrap`
  - Line 3: `from views import clock_view, system_view`
  - The entire `refresh(oled)` function (currently lines 48-70).

  Keep byte-identical (do not touch): the `from sh1107 import WIDTH, HEIGHT` / `import icons` / `import text_render` / `import time` import lines; module-level constants (`_cached_temp`, `_cached_code`, `_cached_is_day`, `_cache_status`, `_REFRESH_MS`, `_RETRY_MS`, `_last_refresh_ms`); `_center_text`; `should_refresh(now_ms)`; `render(oled)`.

  Add a new `set_data(ip, temp, code, is_day)` function in place of the removed `refresh`. Contract:
  - No `oled` parameter (deliberate signature change — set_data has no render responsibility).
  - Declare the same `global` names as the old refresh did (`_cached_temp, _cached_code, _cached_is_day, _cache_status, _last_refresh_ms`).
  - First line MUST be `_last_refresh_ms = time.ticks_ms()` — D-31 stamp-at-start guarantee: a failed fetch still consumes one refresh window before the next attempt.
  - If `ip` is falsy → set `_cache_status = "no_wifi"` and return.
  - Else if `temp is None` → set `_cache_status = "no_data"` and return.
  - Else → assign `_cached_temp = temp`, `_cached_code = code`, `_cached_is_day = is_day`, `_cache_status = "ok"`.
  - Do NOT call `render(oled)` — that responsibility now lives in main.
  - Do NOT import or reference `clock_view`, `system_view`, or `bootstrap`.

  Include a one-line comment above `set_data` explaining its role as a pure state-setter driven by `main._refresh_all`, mirroring the docstring-free / inline-comment style already used elsewhere in the module.

  NOTE: after this task main.py is temporarily broken because `weather_view.refresh` no longer exists — the tree is not in a runnable state until Task 2 lands. This is expected. Do NOT attempt to import or run main.py in this task's verify block; use Task 2's verify (which runs `python3 -c "import ast; ast.parse(open('main.py').read())"` after main.py is updated) to confirm the tree parses again.</action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('views/weather_view.py').read()); print('OK')" | grep -q OK &amp;&amp; \
grep -c "^import bootstrap$" views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -c "^from views import" views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -c "def set_data(ip, temp, code, is_day):" views/weather_view.py | grep -qx 1 &amp;&amp; \
grep -c "def refresh(oled):" views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -c "clock_view\." views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -c "system_view\." views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -c "bootstrap\." views/weather_view.py | grep -qx 0 &amp;&amp; \
grep -n "_last_refresh_ms = time.ticks_ms()" views/weather_view.py &amp;&amp; \
echo "T1_VERIFY_OK"</automated>
  </verify>
  <done>views/weather_view.py parses; has no `import bootstrap` line; has no `from views import ...` line; defines `set_data(ip, temp, code, is_day)`; no longer defines `refresh(oled)`; contains no references to `clock_view.`, `system_view.`, or `bootstrap.`; the D-31 stamp-at-start line `_last_refresh_ms = time.ticks_ms()` is present inside set_data. `should_refresh`, `render`, `_center_text`, and all module-level constants are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update main.py — import bootstrap; add _refresh_all(oled) composition-root helper; migrate both call sites; patch stale sibling-view comment lines</name>
  <files>main.py, views/clock_view.py, views/system_view.py</files>
  <action>Edit `main.py` to make it the composition root for the boot + refresh fan-out, and patch two stale doc-comment lines in the sibling view modules.

  In `main.py`:

  1. Add `import bootstrap` at the top of the file. Place it alongside the existing sh1107/machine/views/text_render imports (natural spot: after `import text_render` on line 4, so the import cluster reads `from sh1107 import OLED, WIDTH, HEIGHT` / `from machine import Pin` / `from views import weather_view, clock_view, system_view` / `import text_render` / `import bootstrap` / `import time`). Alphabetical grouping is not enforced elsewhere in the codebase; keep the existing ordering style.

  2. Add the composition-root helper `_refresh_all(oled)` immediately after `_draw_page_dots` and before the `if __name__ == "__main__":` block (i.e., insert between current lines 71 and 74). Body must be exactly the fan-out sequence:

     ```
     def _refresh_all(oled):
         # Composition-root fan-out: bootstrap fetches once; each view's public
         # setter absorbs its field. weather_view then paints the panel. main
         # overlays the current view before the final show() at each call site
         # (see boot-fetch and scheduler-tick blocks below).
         ip, temp, code, is_day, tz_offset, wan_ip = bootstrap.fetch()
         weather_view.set_data(ip, temp, code, is_day)
         clock_view.set_tz_offset(tz_offset)
         system_view.set_wan_ip(wan_ip)
         weather_view.render(oled)
     ```

  3. Replace both `weather_view.refresh(oled)` call sites:
     - Boot fetch (currently line 95, inside the `__main__` block after the pre-fetch render + dots + show): `weather_view.refresh(oled)` → `_refresh_all(oled)`. Keep the surrounding `_draw_page_dots(oled, _current_idx)` + `oled.show()` lines (96-97) byte-identical.
     - Scheduler tick (currently line 112, inside `if weather_view.should_refresh(now):`): `weather_view.refresh(oled)` → `_refresh_all(oled)`. Keep the surrounding overlay block byte-identical (lines 113-118: comment + `VIEWS[_current_idx].render(oled)` + dots + show).

  4. Preserve everything else in `main.py` unchanged: user config block, private tunables, carousel/IRQ state, `VIEWS` tuple, `_center_text`, `secrets` fallback, `_on_key0`, `_on_key1`, `_draw_page_dots`, the pre-fetch `weather_view.render(oled)` on line 88, the `clock_view.sync(oled)` boot call, the `while True` poll loop's carousel navigation branch (`if _pending_dir != 0`), the clock tick branch (`if _current_idx == 1 and clock_view.should_tick(now)`), the clock sync branch (`if clock_view.should_sync(now)`), and the final `time.sleep_ms(_POLL_MS)`.

  5. The comment on lines 84-87 (about pre-fetch render + "connecting..." from cache_status) may need a light one-line adjustment for accuracy (e.g., updating any incidental reference to `weather_view.refresh` if present in the comment text). Do NOT rewrite it wholesale. If the existing comment reads accurately after the change, leave it alone.

  In `views/clock_view.py`:

  6. Patch the doc-comment on line 34 (inside `set_tz_offset`). Current text: `# Public setter called by weather_view.refresh after each successful` → new text: `# Public setter called by main._refresh_all after each successful`. Preserve the rest of the comment block (lines 35-37) byte-identical. Do NOT change any code — comment-line edit only.

  In `views/system_view.py`:

  7. Patch the doc-comment on line 32 (inside `set_wan_ip`). Current text: `# Public setter called by weather_view.refresh after each successful weather` → new text: `# Public setter called by main._refresh_all after each successful weather`. Preserve the rest of the comment block (lines 33-34) byte-identical. Do NOT change any code — comment-line edit only.

  Explicitly out of scope for Task 2: any change to `bootstrap.py`, any change to `views/weather_view.py` (already handled by Task 1), any change to the code (not comment) of `clock_view.py` or `system_view.py`, any change to `.planning/codebase/*.md`.</action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('main.py').read()); ast.parse(open('views/clock_view.py').read()); ast.parse(open('views/system_view.py').read()); print('OK')" | grep -q OK &amp;&amp; \
grep -c "^import bootstrap$" main.py | grep -qx 1 &amp;&amp; \
grep -c "^def _refresh_all(oled):" main.py | grep -qx 1 &amp;&amp; \
grep -c "weather_view\.refresh(" main.py | grep -qx 0 &amp;&amp; \
[ "$(grep -c '_refresh_all(oled)' main.py)" -ge 2 ] &amp;&amp; \
grep -c "bootstrap\.fetch()" main.py | grep -qx 1 &amp;&amp; \
grep -c "weather_view\.set_data(ip, temp, code, is_day)" main.py | grep -qx 1 &amp;&amp; \
grep -c "clock_view\.set_tz_offset(tz_offset)" main.py | grep -qx 1 &amp;&amp; \
grep -c "system_view\.set_wan_ip(wan_ip)" main.py | grep -qx 1 &amp;&amp; \
grep -c "weather_view\.render(oled)" main.py | grep -qx 2 &amp;&amp; \
grep -c "called by main._refresh_all" views/clock_view.py | grep -qx 1 &amp;&amp; \
grep -c "called by main._refresh_all" views/system_view.py | grep -qx 1 &amp;&amp; \
grep -c "called by weather_view.refresh" views/clock_view.py | grep -qx 0 &amp;&amp; \
grep -c "called by weather_view.refresh" views/system_view.py | grep -qx 0 &amp;&amp; \
git diff --stat bootstrap.py | grep -qv "bootstrap.py" &amp;&amp; \
echo "T2_VERIFY_OK"</automated>
  </verify>
  <done>All three touched Python files parse. `main.py` has exactly one `import bootstrap` line, defines `_refresh_all(oled)` exactly once, calls `_refresh_all(oled)` at ≥2 call sites, has zero remaining `weather_view.refresh(` references, and contains exactly one call to each of `bootstrap.fetch()`, `weather_view.set_data(...)`, `clock_view.set_tz_offset(tz_offset)`, `system_view.set_wan_ip(wan_ip)`, plus exactly two `weather_view.render(oled)` calls (one on line 88 pre-fetch, one inside `_refresh_all`). `views/clock_view.py` and `views/system_view.py` each have exactly one updated comment line referencing `main._refresh_all` and zero remaining references to `weather_view.refresh`. `bootstrap.py` shows no diff (unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Patch CLAUDE.md architecture prose + append STATE.md decision entry documenting the composition-root move</name>
  <files>CLAUDE.md, .planning/STATE.md</files>
  <action>Surgical documentation patches to reflect the new flow.

  In `CLAUDE.md`:

  1. Fix the "Import Organization" bullet for `views/weather_view.py` (line 166). Current text: `` `views/weather_view.py`: `from sh1107 import WIDTH, HEIGHT` → `import bootstrap` → `from views import clock_view, system_view` → `import icons` → `import text_render` → `import time` ``. New text: `` `views/weather_view.py`: `from sh1107 import WIDTH, HEIGHT` → `import icons` → `import text_render` → `import time` ``. (Remove the `import bootstrap` and `from views import clock_view, system_view` segments only; leave the rest of the arrow chain intact.)

  2. Update the "Import Organization" bullet for `main.py` (line 163). Current text ends with `... → `import text_render` → `import time` ``. New text inserts `import bootstrap` between `text_render` and `time`: `... → `import text_render` → `import bootstrap` → `import time` ``. Match the actual placement chosen in Task 2's step 1.

  3. In the "Component Responsibilities" table (around line 247), update the `Application` row's Responsibility cell. Current text: `User configuration, UI rendering loop, weather fetch orchestration`. New text: `User configuration, UI rendering loop, composition-root fetch fan-out (`_refresh_all`)`. Keep the file cell (`main.py`) unchanged; do NOT touch other table rows.

  4. In the layered architecture prose (around line 266-268), keep the existing wording; only if a line explicitly says something like "weather_view orchestrates the fetch + cross-view dispatch" or equivalent, patch it to point at `main._refresh_all` instead. If no such line exists, no edit is needed here — do not fabricate one. The executor should grep for `weather_view.refresh|orchestrat|dispatch|fan-out` in CLAUDE.md and only patch lines that are made inaccurate by this refactor.

  5. Do NOT re-architect any prose. Do NOT touch unrelated sections (Naming Patterns, Code Style, SH1107 gotchas, Hardware Pinout, Development Workflow, etc.). Do NOT touch `.planning/codebase/*.md` (explicitly out of scope — flagged as stale-doc debt to be regenerated by /gsd:map-codebase later).

  In `.planning/STATE.md`:

  6. Append a new decision entry to the "Accumulated Context → Decisions" list. Insert it as a new bullet immediately after the existing "Phase 4 (D-45): System view is READ-ONLY..." bullet (currently line 90) and before the `### Pending Todos` heading (currently line 92). Text:

     `- Cross-phase (2026-07-19, quick 260719-f0b): Cross-view setter dispatch moved out of weather_view.refresh into main._refresh_all. weather_view.refresh(oled) removed; replaced by weather_view.set_data(ip, temp, code, is_day) — a pure state-setter with no oled parameter and no cross-view calls. main.py now imports bootstrap and owns the boot + scheduler-tick fan-out to weather_view.set_data + clock_view.set_tz_offset + system_view.set_wan_ip + weather_view.render. bootstrap.py unchanged.`

  7. Append a new row to the "Quick Tasks Completed" table (currently ends around line 114). Row:

     `| 260719-f0b | Decouple weather_view from sibling views; move cross-view setter dispatch to main._refresh_all | 2026-07-19 | — | [260719-f0b-decouple-weather-view-from-sibling-views](./quick/260719-f0b-decouple-weather-view-from-sibling-views/) |`

     (The commit column stays `—` until the operator commits.)

  8. Do NOT rewrite the D-31/D-38/D-41 entries — they are historical record. Do NOT touch the frontmatter, Performance Metrics, Deferred Items, Session Continuity, or any other section. Do NOT bump progress counters (this is a quick task, not a phase).</action>
  <verify>
    <automated>grep -c "import bootstrap.*from views import clock_view" CLAUDE.md | grep -qx 0 &amp;&amp; \
grep -c "\`views/weather_view.py\`:" CLAUDE.md | grep -qx 1 &amp;&amp; \
grep -c "import bootstrap" CLAUDE.md | grep -q . &amp;&amp; \
grep -c "_refresh_all" CLAUDE.md | grep -q . &amp;&amp; \
grep -c "260719-f0b" .planning/STATE.md | grep -q . &amp;&amp; \
grep -c "main._refresh_all" .planning/STATE.md | grep -qx 1 &amp;&amp; \
grep -c "weather_view.set_data" .planning/STATE.md | grep -qx 1 &amp;&amp; \
grep -c "260719-f0b-decouple-weather-view-from-sibling-views" .planning/STATE.md | grep -qx 1 &amp;&amp; \
[ "$(git diff --stat bootstrap.py | wc -l)" -eq 0 ] &amp;&amp; \
echo "T3_VERIFY_OK"</automated>
  </verify>
  <done>CLAUDE.md's `views/weather_view.py` import bullet no longer lists `import bootstrap` or `from views import clock_view, system_view`. CLAUDE.md's `main.py` import bullet now includes `import bootstrap`. The Application row's Responsibility cell references `_refresh_all` (composition-root fan-out). No unrelated CLAUDE.md sections are modified. `.planning/STATE.md` has the new decision bullet (containing "260719-f0b", "main._refresh_all", and "weather_view.set_data") and the new Quick Tasks Completed row (containing "260719-f0b-decouple-weather-view-from-sibling-views"). Frontmatter, Progress, Performance Metrics, and prior decision entries are unchanged. `bootstrap.py` shows zero diff since the start of the refactor.</done>
</task>

</tasks>

<verification>
End-to-end static verification after all three tasks complete (run from repo root):

```bash
# 1. Every touched Python file parses.
python3 -c "import ast; \
  ast.parse(open('views/weather_view.py').read()); \
  ast.parse(open('main.py').read()); \
  ast.parse(open('views/clock_view.py').read()); \
  ast.parse(open('views/system_view.py').read()); \
  ast.parse(open('bootstrap.py').read()); \
  print('all parse OK')"

# 2. weather_view is decoupled — zero sibling or bootstrap imports.
grep -n "^import bootstrap\|^from views import" views/weather_view.py   # expect: no output
grep -c "clock_view\.\|system_view\.\|bootstrap\." views/weather_view.py # expect: 0

# 3. main.py is the composition root.
grep -c "^import bootstrap$" main.py                    # expect: 1
grep -c "^def _refresh_all(oled):" main.py              # expect: 1
grep -c "weather_view\.refresh(" main.py                # expect: 0
grep -c "_refresh_all(oled)" main.py                    # expect: >= 2 (definition + 2 call sites = 3)
grep -n "bootstrap\.fetch()\|weather_view\.set_data\|clock_view\.set_tz_offset\|system_view\.set_wan_ip\|weather_view\.render" main.py

# 4. Sibling comment lines updated (code unchanged).
grep -c "called by main._refresh_all" views/clock_view.py   # expect: 1
grep -c "called by main._refresh_all" views/system_view.py  # expect: 1
grep -c "called by weather_view.refresh" views/clock_view.py  views/system_view.py  # expect both 0

# 5. bootstrap.py is byte-identical.
git diff --stat bootstrap.py                            # expect: no output

# 6. Documentation is updated.
grep -n "260719-f0b\|main._refresh_all\|weather_view.set_data" .planning/STATE.md
grep -n "_refresh_all" CLAUDE.md
```

No on-device runtime verification in this plan — this repo has no host-side tests, and the operator will do the mpremote copy + boot check manually. The static grep + ast.parse gates above are sufficient to prove the refactor is structurally sound; runtime behavior is unchanged by construction because (a) the failure semantics inside set_data are byte-for-byte identical to the old refresh's decision tree, (b) the call ordering inside `_refresh_all` matches the old inline sequence in `refresh` exactly, and (c) both `render()` and `should_refresh()` are untouched.
</verification>

<success_criteria>
- `views/weather_view.py` imports only `sh1107`, `icons`, `text_render`, `time` (no `bootstrap`, no `views.*`).
- `views/weather_view.py` defines `set_data(ip, temp, code, is_day)`; does not define `refresh(oled)`; `should_refresh` and `render` are byte-identical to pre-refactor.
- `main.py` imports `bootstrap`; defines `_refresh_all(oled)`; both former `weather_view.refresh(oled)` call sites now call `_refresh_all(oled)`; every other line in `main.py` is byte-identical to pre-refactor.
- `views/clock_view.py` and `views/system_view.py` have exactly one comment-line update each (setter doc-string now says "called by main._refresh_all"); all executable code unchanged.
- `bootstrap.py` shows zero diff versus pre-refactor.
- `CLAUDE.md` has surgical patches: `views/weather_view.py` import bullet trimmed of the removed imports; `main.py` import bullet includes `import bootstrap`; Application row references `_refresh_all` fan-out. No other CLAUDE.md changes.
- `.planning/STATE.md` has a new cross-phase decision entry (dated 2026-07-19, quick 260719-f0b) documenting the move, and a new row in the Quick Tasks Completed table. Frontmatter and other sections untouched.
- All five touched Python files parse under `ast.parse`.
- No modifications to `.planning/codebase/*.md`.
</success_criteria>

<output>
On completion, write summary to `.planning/quick/260719-f0b-decouple-weather-view-from-sibling-views/260719-f0b-SUMMARY.md` following the standard quick-task summary template. Include:
- The three-task breakdown as executed (Task 1 refactor, Task 2 main.py + comment patches, Task 3 doc updates).
- Line counts / diff shape for each touched file.
- Verification output (the six grep/ast commands from `<verification>`).
- Note that runtime verification is deferred to the operator (mpremote copy + on-device boot).
- Reminder that `.planning/codebase/*.md` remains stale-doc debt to be regenerated by /gsd:map-codebase before v2 scoping.
</output>
