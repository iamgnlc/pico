---
phase: quick-260719-e5g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - views/__init__.py
  - views/weather_view.py
  - views/clock_view.py
  - views/system_view.py
  - main.py
  - CLAUDE.md
autonomous: true
requirements:
  - QUICK-260719-e5g-01
  - QUICK-260719-e5g-02
  - QUICK-260719-e5g-03

must_haves:
  truths:
    - "The three view modules live under a `views/` package with git history preserved (git log --follow shows continuity through the rename)."
    - "`main.py` and the moved `views/weather_view.py` import via the new path, and existing call sites (`weather_view.render(oled)`, `clock_view.should_tick(now)`, `system_view.set_wan_ip(...)`, etc.) keep working unchanged."
    - "`CLAUDE.md` prose no longer references the flat `weather_view.py` / `clock_view.py` / `system_view.py` paths; every mention is `views/<name>.py`."
  artifacts:
    - path: "views/__init__.py"
      provides: "Empty package marker so MicroPython treats `views/` as a package"
    - path: "views/weather_view.py"
      provides: "Weather view module, moved from repo root with `git mv`"
      contains: "def render(oled)"
    - path: "views/clock_view.py"
      provides: "Clock view module, moved from repo root with `git mv`"
      contains: "def render(oled)"
    - path: "views/system_view.py"
      provides: "System view module, moved from repo root with `git mv`"
      contains: "def render(oled)"
    - path: "main.py"
      provides: "Updated top-of-file import to package form"
      contains: "from views import weather_view, clock_view, system_view"
    - path: "views/weather_view.py"
      provides: "Updated intra-package sibling imports"
      contains: "from views import clock_view, system_view"
  key_links:
    - from: "main.py"
      to: "views/weather_view.py, views/clock_view.py, views/system_view.py"
      via: "from views import weather_view, clock_view, system_view"
      pattern: "from views import weather_view, clock_view, system_view"
    - from: "views/weather_view.py"
      to: "views/clock_view.py, views/system_view.py"
      via: "sibling package import"
      pattern: "from views import clock_view, system_view"
    - from: "main.py:VIEWS tuple + call sites (lines 28, 90, 97, 103, 110, 113-126)"
      to: "moved view modules"
      via: "short-name references (`weather_view.render(...)`, `clock_view.should_tick(...)`, etc.)"
      pattern: "(weather_view|clock_view|system_view)\\."
---

<objective>
Move the three flat view modules (`weather_view.py`, `clock_view.py`, `system_view.py`) into a `views/` package so the repo root only carries the driver, entry point, and shared helpers. This is a pure code-organization change — no behavior, no new features, no rendering changes.

Purpose: Reduce repo-root noise ahead of v2 scoping; establish a package for future view additions (e.g. Phase 4-style read-only info views) without renaming call sites.

Output:
- New `views/` package with the three modules relocated (history preserved via `git mv`).
- `main.py` and `views/weather_view.py` imports switched to `from views import ...` so existing short-name call sites keep working unchanged.
- `CLAUDE.md` architecture prose updated to reference the new paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./main.py
@./weather_view.py
@./clock_view.py
@./system_view.py

<interfaces>
<!-- Existing call sites in main.py that MUST keep working unchanged after the move. -->
<!-- These reference the modules by short name, which is why the import form must be -->
<!-- `from views import weather_view, clock_view, system_view` (not `import views.weather_view`). -->

From main.py (call sites — do NOT modify these lines):
- Line 28:  `VIEWS = (weather_view, clock_view, system_view)`
- Line 90:  `weather_view.render(oled)`
- Line 97:  `weather_view.refresh(oled)`
- Line 103: `clock_view.sync(oled)`
- Line 110: `VIEWS[_current_idx].render(oled)`
- Line 113: `weather_view.should_refresh(now)`
- Line 114: `weather_view.refresh(oled)`
- Line 121: `clock_view.should_tick(now)`
- Line 122: `clock_view.render(oled)`
- Line 125: `clock_view.should_sync(now)`
- Line 126: `clock_view.sync(oled)`

From weather_view.py (call sites — do NOT modify these lines after the file is moved):
- Line 69: `clock_view.set_tz_offset(tz_offset)`
- Line 70: `system_view.set_wan_ip(wan_ip)`

Public module surface (unchanged by this plan):
- `weather_view`: `render(oled)`, `refresh(oled)`, `should_refresh(now_ms)`
- `clock_view`: `render(oled)`, `sync(oled)`, `should_tick(now_ms)`, `should_sync(now_ms)`, `set_tz_offset(offset)`
- `system_view`: `render(oled)`, `set_wan_ip(ip)`

CLAUDE.md sections referencing the flat paths (to be patched in T-3):
- "Frameworks" section — `bootstrap.py:1` mentions are fine; the view files are referenced in the "Naming Patterns" list ("`weather_view.py`, `clock_view.py`, `system_view.py`")
- "Import Organization" — the `weather_view.py:` line documenting its imports
- "Module Design" — the `weather.py exports`, `wifi.py exports` items already reference retired filenames (out of scope — do NOT fix those; only touch the three view-file references)
- "Component Responsibilities" table — the "File" column entries for the three views
- "Layers" section — the `Location:` lines for the view modules
</interfaces>

Notes on MicroPython behavior:
- MicroPython supports packages via an empty `__init__.py` (same as CPython). No `sys.path` gymnastics needed — the `views/` directory alongside `main.py` on the Pico filesystem is enough.
- `from views import weather_view, clock_view, system_view` binds the three modules to local names `weather_view`, `clock_view`, `system_view`, so every existing short-name call site keeps working.
- No host tests exist; verification is (a) `ast.parse` on each touched .py file and (b) grep confirming no stale bare `import weather_view|clock_view|system_view` lines remain outside `views/`. The operator does on-device runtime verification manually via `mpremote`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create views/ package and git-mv the three view modules</name>
  <files>views/__init__.py, views/weather_view.py, views/clock_view.py, views/system_view.py</files>
  <action>
    Create the `views/` directory and populate it:
    1. Run `mkdir -p views` at the repo root.
    2. Create `views/__init__.py` as an EMPTY file (zero bytes, or a single trailing newline is acceptable). Do NOT add any imports, docstrings, or `__all__` — it exists only as a package marker.
    3. Move the three view modules preserving git history — use `git mv`, NOT plain `mv` or `cp`:
       - `git mv weather_view.py views/weather_view.py`
       - `git mv clock_view.py   views/clock_view.py`
       - `git mv system_view.py  views/system_view.py`
    4. Do NOT edit the contents of the moved files in this task — the intra-package import fix inside `views/weather_view.py` belongs to T-2 to keep the git-mv rename detection clean (renames + edits in a single commit still work but the diff is noisier; splitting into T-1 pure-rename + T-2 edits keeps the history readable).
    5. Do NOT rename the files (they stay `weather_view.py` / `clock_view.py` / `system_view.py` inside `views/`).
  </action>
  <verify>
    <automated>test -f views/__init__.py && test -f views/weather_view.py && test -f views/clock_view.py && test -f views/system_view.py && ! test -f weather_view.py && ! test -f clock_view.py && ! test -f system_view.py &amp;&amp; git status --porcelain | grep -E "^R" | grep -E "views/(weather|clock|system)_view.py" | wc -l | tr -d ' '</automated>
    Expected: exit 0, and the final `wc -l` prints `3` (three renames detected by git). If it prints anything other than `3`, git did not detect the renames as such — investigate before proceeding.
  </verify>
  <done>
    `views/` exists with `__init__.py` (empty) plus the three moved view modules. The three original flat files no longer exist at repo root. `git status` shows the moves as renames (R100 or similar) rather than delete + add.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite imports in main.py and views/weather_view.py</name>
  <files>main.py, views/weather_view.py</files>
  <action>
    Update the import statements — and ONLY the import statements — in the two files that reference the view modules by bare name. Do NOT touch any call sites, function bodies, or other code.

    In `main.py`, lines 3-5 currently read:
    ```
    import weather_view
    import clock_view
    import system_view
    ```
    Replace those three lines with a single line:
    ```
    from views import weather_view, clock_view, system_view
    ```
    Preserve the surrounding imports (`from sh1107 import ...` on line 1, `from machine import Pin` on line 2, `import text_render` and `import time` on lines 6-7). The `VIEWS = (weather_view, clock_view, system_view)` tuple on line 28 and every subsequent call site (see the `<interfaces>` block for the full list) MUST remain byte-identical.

    In `views/weather_view.py` (after the T-1 move), lines 3-4 currently read:
    ```
    import clock_view
    import system_view
    ```
    Replace those two lines with a single line:
    ```
    from views import clock_view, system_view
    ```
    Preserve `from sh1107 import WIDTH, HEIGHT` (line 1), `import bootstrap` (line 2), `import icons` (line 5), `import text_render` (line 6), `import time` (line 7). Lines 69-70 (`clock_view.set_tz_offset(...)`, `system_view.set_wan_ip(...)`) MUST remain byte-identical — the sibling short-name calls stay because `from views import clock_view, system_view` binds those names in this module's namespace exactly the way the original bare `import` did.

    Rationale for `from views import ...` (not `import views.weather_view as weather_view`): the former binds the short module name in one line for all three modules and matches the existing call-site pattern (`weather_view.render(...)`), so no call sites need to change. Do NOT use `from views.weather_view import *` or any other form — the package-import form is deliberate.

    Do NOT touch `views/clock_view.py` or `views/system_view.py` — those files have no imports of the other view modules, so they need no changes.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('main.py').read()); ast.parse(open('views/weather_view.py').read()); ast.parse(open('views/clock_view.py').read()); ast.parse(open('views/system_view.py').read()); print('OK')" &amp;&amp; test "$(grep -rn --include='*.py' -E '^import (weather_view|clock_view|system_view)$' . | grep -v '^./views/' | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -q "^from views import weather_view, clock_view, system_view$" main.py &amp;&amp; grep -q "^from views import clock_view, system_view$" views/weather_view.py</automated>
    Expected: exit 0, `OK` printed, no stale bare `import weather_view|clock_view|system_view` lines anywhere outside `views/`, and both new import lines present exactly as written.
  </verify>
  <done>
    All four Python files (main.py + three view modules) parse cleanly. `main.py` imports the trio from the `views` package. `views/weather_view.py` imports its two siblings from the `views` package. Zero call sites modified. Zero bare short-name imports remain outside `views/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch CLAUDE.md path references</name>
  <files>CLAUDE.md</files>
  <action>
    Update every mention of the three flat view-module paths in `CLAUDE.md` to their new `views/`-prefixed form. Do NOT rewrite the surrounding prose — this is a surgical path patch, not an architecture-doc refresh.

    Scope of edits (use `grep -n` first to find every hit before editing so you don't miss one):
    ```
    grep -n -E "(weather_view|clock_view|system_view)\.py" CLAUDE.md
    ```

    For every match, replace bare `weather_view.py` → `views/weather_view.py`, bare `clock_view.py` → `views/clock_view.py`, bare `system_view.py` → `views/system_view.py`. Preserve backtick formatting, list markers, and inline punctuation. Known sections that will need edits based on the current file:
    - The "Naming Patterns" list ("view modules `weather_view.py`, `clock_view.py`, `system_view.py`")
    - The "Import Organization" section — the line documenting `weather_view.py:`'s import order (rename to `views/weather_view.py:`)
    - The "Component Responsibilities" table — the `File` column entries for Weather View, Clock View, System View
    - The "Layers" section — the `Location:` lines for each of the three view modules
    - Any other spot the grep above surfaces

    Explicitly out of scope (do NOT touch):
    - `.planning/codebase/*.md` — flagged as stale-doc technical debt in the anti-patterns list; will be regenerated by `/gsd:map-codebase` before v2 scoping.
    - Stale references in CLAUDE.md to already-retired filenames like `weather.py` / `wifi.py` (from before the bootstrap rename) — those are pre-existing debt unrelated to this move.
    - Any prose describing the view module *responsibilities*, *interface*, or *behavior* — only the file *path* strings change.

    If a reference appears inside a mermaid diagram or code fence, still update it — but leave the diagram structure untouched.
  </action>
  <verify>
    <automated>test "$(grep -c -E '(^| )(weather_view|clock_view|system_view)\.py' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -c -E 'views/(weather_view|clock_view|system_view)\.py' CLAUDE.md)" -ge "3"</automated>
    Expected: exit 0. First check confirms no bare `weather_view.py|clock_view.py|system_view.py` mentions remain (word-boundary anchored on left so `views/weather_view.py` doesn't false-positive). Second check confirms at least three new `views/...` references exist. If the first count is not `0`, run the grep manually to locate the missed spot.
  </verify>
  <done>
    `CLAUDE.md` has zero remaining bare `weather_view.py|clock_view.py|system_view.py` references; every mention now uses the `views/`-prefixed path. Surrounding prose unchanged. `.planning/codebase/*.md` untouched.
  </done>
</task>

</tasks>

<verification>
Whole-plan verification (run after all three tasks are done):

1. **Layout** — `views/` package present with 4 files:
   ```
   ls views/
   ```
   Expected output includes exactly: `__init__.py`, `weather_view.py`, `clock_view.py`, `system_view.py`.

2. **No flat leftovers** at repo root:
   ```
   ls weather_view.py clock_view.py system_view.py 2>&1 | grep -c "No such file"
   ```
   Expected: `3` (all three no longer exist at root).

3. **Static syntax check** on every touched Python file:
   ```
   python3 -c "import ast; [ast.parse(open(f).read()) for f in ['main.py','views/__init__.py','views/weather_view.py','views/clock_view.py','views/system_view.py']]; print('all-parse-ok')"
   ```
   Expected: prints `all-parse-ok` and exit 0.

4. **Import shape** — the two rewritten import lines exist verbatim:
   ```
   grep -q "^from views import weather_view, clock_view, system_view$" main.py &&
   grep -q "^from views import clock_view, system_view$" views/weather_view.py &&
   echo "imports-ok"
   ```
   Expected: prints `imports-ok`.

5. **No stale bare imports** anywhere outside the `views/` package:
   ```
   grep -rn --include='*.py' -E "^import (weather_view|clock_view|system_view)$" . | grep -v "^./views/"
   ```
   Expected: no output (exit 1).

6. **Call sites unchanged** — spot-check that the short-name pattern still dominates:
   ```
   grep -c -E "(weather_view|clock_view|system_view)\." main.py
   ```
   Expected: >= 10 (the tuple + all render / refresh / sync / should_* / set_* calls). Confirms no accidental rewrite of call sites into `views.weather_view.render(...)` form.

7. **Docs patched** — CLAUDE.md path grep:
   ```
   grep -c -E "views/(weather_view|clock_view|system_view)\.py" CLAUDE.md
   ```
   Expected: >= 3.

8. **On-device runtime verification** (out of scope for this plan — operator does this manually):
   The operator will `mpremote cp -r views/ main.py sh1107.py bootstrap.py icons.py text_render.py :` and confirm boot, view carousel, and 10-min refresh still work. The plan does NOT drive this and does NOT block on it.
</verification>

<success_criteria>
- `views/` package exists with `__init__.py` + the three relocated modules; git detects the three moves as renames (T-1 verify grep).
- `main.py` and `views/weather_view.py` import via `from views import ...`; every existing call site is byte-identical (T-2 verify + grep counts).
- `CLAUDE.md` prose now references `views/<name>.py` everywhere the flat paths used to appear (T-3 verify).
- All four touched Python files parse via `ast.parse` (whole-plan verification #3).
- Zero host tests were added or run (this repo has none by design).
- On-device runtime verification is deferred to the operator per repo convention.
</success_criteria>

<output>
Create `.planning/quick/260719-e5g-move-view-modules-to-views-subdirectory/260719-e5g-SUMMARY.md` when done, following the standard summary template. Include a "Files renamed" section with the `git mv` pairs for future traceability.
</output>
