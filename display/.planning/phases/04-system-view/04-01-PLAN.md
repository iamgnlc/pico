---
phase: 04-system-view
plan: 01
type: execute
wave: 1
files_modified:
  - weather.py
  - weather_view.py
  - system_view.py
autonomous: false
requirements:
  - SYSTEM-01
  - SYSTEM-02
  - SYSTEM-03
  - SYSTEM-04
tags:
  - micropython
  - pico-w
  - wifi
  - diagnostics
  - system

must_haves:
  truths:
    - "weather.py URL includes `?fields=lat,lon,offset,query` (query = ip-api's field name for the client's WAN IP)"
    - "weather.current() returns a 5-tuple (temp, code, is_day, tz_offset, wan_ip)"
    - "weather.current() failure path returns (None, None, None, None, None)"
    - "weather_view.refresh unpacks 5 values, calls clock_view.set_tz_offset(tz_offset) AND system_view.set_wan_ip(wan_ip) on the successful path"
    - "weather_view imports system_view at module top (no circular: system_view does NOT import weather_view)"
    - "system_view.py defines module state `_cached_wan_ip` (initially None)"
    - "system_view.py provides public setter `set_wan_ip(ip)` that: (a) returns early on ip is None, (b) returns early on ip == _cached_wan_ip, (c) updates _cached_wan_ip"
    - "system_view.set_wan_ip does NOT write to any file (RAM-only cache per D-43-bis)"
    - "system_view.render(oled) reads network.WLAN(network.STA_IF) primitives inline: isconnected(), config('essid'), status('rssi')"
    - "system_view.render(oled) branches on `wlan.isconnected()`: online → show SSID/IP/bars; offline → show `SSID: --`, `IP: --`, all bars hollow"
    - "system_view.render(oled) shows `IP: --` when either offline OR `_cached_wan_ip is None` (dual gate)"
    - "system_view.render(oled) uses `.format()` for all string interpolation; no f-strings, no type hints, no docstrings (CONVENTIONS.md)"
    - "SSID is truncated to min(15, WIDTH // 8) chars with NO ellipsis (matches prior truncation pattern)"
    - "Signal bars are drawn as 4 filled/hollow rectangles per D-42 RSSI thresholds: >= -55 → 4 bars, -55..-65 → 3, -65..-75 → 2, < -75 → 1, offline → 0"
    - "Layout: SSID at y=8 left-aligned x=0, IP at y=24 left-aligned x=0, `Signal ` label at y=40 x=0 followed by 4 bar rectangles"
    - "Content stays in rows 0-53 (Phase 2 D-19 constraint)"
    - "clock_view.py, main.py, sh1107.py, wifi.py, icons.py, text_render.py, secrets.py, .gitignore are NOT modified"
    - "No new imports beyond `network` and `text_render` in system_view.py; no new imports at all in weather.py"
    - "No poll-loop changes in main.py — Phase 4 has NO should_tick or should_sync predicate"
  artifacts:
    - path: "weather.py"
      provides: "5-tuple return with wan_ip piggybacked from ip-api's `query` field"
      contains: 'fields=lat,lon,offset,query'
    - path: "weather_view.py"
      provides: "5-tuple unpack + system_view.set_wan_ip call on success (alongside existing clock_view.set_tz_offset call)"
      contains: "system_view.set_wan_ip(wan_ip)"
    - path: "system_view.py"
      provides: "Full diagnostics view — _cached_wan_ip state, set_wan_ip setter, render(oled) with inline network.WLAN reads and offline branch"
      contains: "_cached_wan_ip"
  key_links:
    - from: "weather.current()"
      to: "ip-api `query` field"
      via: "URL ?fields=... explicitly requests query"
      pattern: "fields=lat,lon,offset,query"
    - from: "weather_view.refresh"
      to: "system_view.set_wan_ip"
      via: "direct module call after successful weather fetch, next to the existing clock_view.set_tz_offset call"
      pattern: "system_view\\.set_wan_ip\\(wan_ip\\)"
    - from: "system_view._cached_wan_ip"
      to: "system_view.render"
      via: "read + branch — combined with wlan.isconnected() forms the dual gate for the IP field"
      pattern: "_cached_wan_ip"
    - from: "main.py carousel branch"
      to: "system_view.render"
      via: "VIEWS[_current_idx].render(oled) — byte-identical to today's call site (no changes to main.py)"
      pattern: "VIEWS\\[_current_idx\\]\\.render"
---

## Phase Goal

**As a** person diagnosing the Pico OLED's network state, **I want to** navigate to the System view and see the SSID, WAN IP, and signal strength at a glance, **so that** I can immediately tell whether the device is connected, to which network, and how strong the signal is — without needing a laptop or REPL.

<objective>
Deliver SYSTEM-01..04 end-to-end in a single plan. `system_view.py` gets its full implementation (from a 2-line stub); `weather.py` extends its return tuple to include the WAN IP piggybacked from ip-api's `query` field; `weather_view.py` unpacks the 5-tuple and forwards the WAN IP to `system_view` via a public setter mirror of the tz_offset pattern.

Purpose: Satisfies SYSTEM-01 (SSID), SYSTEM-02 (IP — interpreted as WAN IP per D-43-bis), SYSTEM-03 (signal as drawn bars per D-42), SYSTEM-04 (uniform `--` offline UX per D-44). Encapsulates all diagnostic display inside `system_view.py`; no changes to `main.py`'s poll loop. System view stays READ-ONLY (D-45) — never touches WiFi lifecycle.

Output: `weather.py` extended URL + return tuple; `weather_view.py` unpack + forward; `system_view.py` full implementation (module state, setter, render with online/offline branch, bar drawing helper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-system-view/04-CONTEXT.md
@.planning/phases/03-clock-view/03-02-PLAN.md
@.planning/phases/03-clock-view/03-CONTEXT.md
@.planning/phases/02-carousel-+-weather/02-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/.continue-here.md
@CLAUDE.md
@weather.py
@weather_view.py
@system_view.py
@clock_view.py
@main.py

<interfaces>
<!-- Contracts assume all prior phases have landed. weather.py currently returns 4-tuple with tz_offset (Plan 03-02 + c23bb5a URL fix); this plan extends to 5-tuple with wan_ip. -->

### ip-api `query` field

ip-api's response includes a `query` field which is the client's public IP as seen by the API server. Same posture as the `offset` field: NOT in the default response, requires explicit `?fields=`. The already-corrected URL becomes:

`http://ip-api.com/json/?fields=lat,lon,offset,query`

Failure mode: if `query` is missing from the response (should never happen when explicitly requested), `loc.get("query")` returns `None`, `set_wan_ip(None)` no-ops. Same graceful degradation as tz_offset.

### Target `weather.py`

Change the URL from `?fields=lat,lon,offset` to `?fields=lat,lon,offset,query`. Add `wan_ip = loc.get("query")` extraction. Extend return tuple to include `wan_ip`. Update failure return to 5 Nones.

```python
def current():
    try:
        # ip-api's default response omits `offset` and `query`; request both explicitly
        # along with lat/lon. Without ?fields=..., the added fields come back as None
        # and downstream setters no-op — persistence + WAN IP display never populate.
        r = urequests.get("http://ip-api.com/json/?fields=lat,lon,offset,query")
        loc = r.json()
        r.close()
        offset = loc.get("offset")
        wan_ip = loc.get("query")
        url = ("https://api.open-meteo.com/v1/forecast"
               "?latitude={}&longitude={}"
               "&current=temperature_2m,weather_code,is_day").format(
            loc["lat"], loc["lon"])
        r = urequests.get(url)
        cur = r.json()["current"]
        r.close()
        return cur["temperature_2m"], cur["weather_code"], cur["is_day"], offset, wan_ip
    except Exception:
        return None, None, None, None, None
```

### Target `weather_view.py` changes

Add `import system_view` at the top (mirror of the existing `import clock_view`). Change the unpack in `refresh(oled)` from 4-tuple to 5-tuple. Add `system_view.set_wan_ip(wan_ip)` after the existing `clock_view.set_tz_offset(tz_offset)` call.

Import placement: alphabetical grouping is loose in the current file (sh1107 → wifi → weather → clock_view → icons → text_render → time). Add `system_view` immediately after `clock_view` and before `icons` (all three are view modules; grouping them keeps the file readable).

### Target `system_view.py` — full replacement

Current file (before this plan):
```python
def render(oled):
    oled.fill(0)
```

Target file:
```python
import network
import text_render
from sh1107 import WIDTH


_cached_wan_ip = None


def _draw_bars(oled, x, y, level):
    # 4 vertical bars, filled if bar index < level, hollow otherwise.
    # Each bar is 4 px wide, 6 px tall, 2 px gap. Total width = 22 px.
    # y is the top of the bars (not baseline).
    for i in range(4):
        bx = x + i * 6
        if i < level:
            oled.fill_rect(bx, y, 4, 6, 1)
        else:
            oled.rect(bx, y, 4, 6, 1)


def _rssi_to_bars(rssi):
    if rssi >= -55:
        return 4
    if rssi >= -65:
        return 3
    if rssi >= -75:
        return 2
    return 1


def set_wan_ip(ip):
    # Public setter called by weather_view.refresh after each successful weather
    # fetch. Idempotent guard: skip on None (failed fetches) and on unchanged
    # value. RAM-only — no file persistence (D-43-bis).
    global _cached_wan_ip
    if ip is None:
        return
    if ip == _cached_wan_ip:
        return
    _cached_wan_ip = ip


def render(oled):
    oled.fill(0)
    wlan = network.WLAN(network.STA_IF)
    connected = wlan.isconnected()

    # SSID line (y=8)
    if connected:
        ssid = wlan.config("essid")
        ssid = ssid[:min(15, WIDTH // 8)]
        text_render.text(oled, "SSID: " + ssid, 0, 8, 1)
    else:
        text_render.text(oled, "SSID: --", 0, 8, 1)

    # IP line (y=24) — WAN IP; shown only when connected AND cache populated
    if connected and _cached_wan_ip is not None:
        text_render.text(oled, "IP: " + _cached_wan_ip, 0, 24, 1)
    else:
        text_render.text(oled, "IP: --", 0, 24, 1)

    # Signal line (y=40) — label + drawn bars
    text_render.text(oled, "Signal ", 0, 40, 1)
    if connected:
        try:
            rssi = wlan.status("rssi")
            level = _rssi_to_bars(rssi)
        except Exception:
            level = 0
    else:
        level = 0
    # Bars start at x=56 (7 chars * 8 px = 56), aligned with the label baseline
    _draw_bars(oled, 56, 40, level)
```

Notes:
- `wlan.config("essid")` may return `bytes` on some MicroPython builds; slicing bytes returns bytes not str. This plan assumes str; the executor should test on-device and, if bytes is observed, add `.decode()` (defensive).
- `wlan.status("rssi")` may raise on some firmware if the argument is unsupported. Wrapped in try/except for safety — falls back to 0 bars.
- The `_center_text` helper is NOT included; system view uses `text_render.text` directly with left-aligned positioning (x=0).
- Content spans y=8..46 (SSID at y=8, IP at y=24, Signal at y=40 with 6-px-tall bars extending to y=46). All inside rows 0-53. Page dots at y=60 unaffected.

### `main.py` — NO CHANGES

Phase 4 makes zero changes to `main.py`. Confirmed:
- No new import (system_view is already imported at line 5 as part of the VIEWS tuple).
- No new user-config constants.
- No new boot-time calls.
- No new poll-loop branches.
- The existing `VIEWS[_current_idx].render(oled)` in the carousel branch already dispatches to `system_view.render` when `_current_idx == 2`.

Verifier will `git diff --exit-code main.py` to prove this.

### Circular-import check

Current chain: `main → weather_view → clock_view` (no back-edges).
New chain: `main → weather_view → clock_view + system_view` (no back-edges — system_view does NOT import weather_view).

`system_view.py` imports: `network`, `text_render`, `WIDTH from sh1107`. None of these transitively import `system_view` or `weather_view`. No circular.

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend weather.py URL and return tuple with wan_ip</name>
  <files>weather.py</files>
  <read_first>
    - weather.py (post-c23bb5a state — 4-tuple with tz_offset from ?fields=lat,lon,offset)
    - .planning/phases/04-system-view/04-CONTEXT.md (D-43-bis WAN IP source)
    - .planning/.continue-here.md (anti-pattern: ip-api default fields omit extended fields — same applies to `query`)
  </read_first>
  <action>
    Modify `weather.py`:

    (a) Change the URL from `http://ip-api.com/json/?fields=lat,lon,offset` to `http://ip-api.com/json/?fields=lat,lon,offset,query`.

    (b) After `offset = loc.get("offset")`, add:
        `wan_ip = loc.get("query")`

    (c) Change the successful return from `return cur["temperature_2m"], cur["weather_code"], cur["is_day"], offset` to:
        `return cur["temperature_2m"], cur["weather_code"], cur["is_day"], offset, wan_ip`

    (d) Change the exception fallback `return None, None, None, None` to:
        `return None, None, None, None, None`

    (e) Update the URL-comment (introduced by c23bb5a) to mention `query` alongside `offset`.

    No other changes. No new imports. Broad-exception fallback preserved.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('weather.py').read()); print('weather.py: syntax ok')"</automated>
    <automated>grep -Eq 'http://ip-api\.com/json/\?fields=lat,lon,offset,query' weather.py && echo "URL requests query field: ok"</automated>
    <automated>grep -Eq 'wan_ip = loc\.get\("query"\)' weather.py && echo "wan_ip extraction: ok"</automated>
    <automated>grep -Eq 'return cur\["temperature_2m"\], cur\["weather_code"\], cur\["is_day"\], offset, wan_ip' weather.py && echo "5-tuple success return: ok"</automated>
    <automated>grep -Eq "return None, None, None, None, None" weather.py && echo "5-tuple failure return: ok"</automated>
    <automated>grep -c "return None, None, None, None," weather.py | grep -Eq "^1$" && echo "no stale 4-tuple returns: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `weather.py` parses.
    - URL requests both `offset` and `query` fields explicitly.
    - Successful return is a 5-tuple ending with `wan_ip`.
    - Failure return is `(None, None, None, None, None)`.
    - No orphan 4-tuple returns.
  </acceptance_criteria>
  <done>weather.py returns the 5-tuple with wan_ip piggybacked from ip-api's `query` field.</done>
</task>

<task type="auto">
  <name>Task 2: Update weather_view.py to unpack 5-tuple and forward wan_ip to system_view</name>
  <files>weather_view.py</files>
  <read_first>
    - weather_view.py (post-quick 260718 state — spinner removed, 4-tuple unpack, calls clock_view.set_tz_offset on success)
    - system_view.py (Task 3 has NOT run yet at read time — but must import will resolve at execution time when Task 3 lands the setter)
  </read_first>
  <action>
    Modify `weather_view.py`:

    (a) Add `import system_view` at the top, placed AFTER the existing `import clock_view` and BEFORE `import icons`. The imports block should read:
        ```
        from sh1107 import WIDTH, HEIGHT
        import wifi
        import weather
        import clock_view
        import system_view
        import icons
        import text_render
        import time
        ```

    (b) Change the line `temp, code, is_day, tz_offset = weather.current()` to:
        `temp, code, is_day, tz_offset, wan_ip = weather.current()`

    (c) After the existing `clock_view.set_tz_offset(tz_offset)` line (currently the last line before `render(oled)`), add:
        `system_view.set_wan_ip(wan_ip)`

    No other changes. Cache tuple, error branches, spinner-free flow all stay byte-identical.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('weather_view.py').read()); print('weather_view.py: syntax ok')"</automated>
    <automated>grep -Eq "^import system_view$" weather_view.py && echo "system_view import: ok"</automated>
    <automated>grep -Eq "temp, code, is_day, tz_offset, wan_ip = weather\.current\(\)" weather_view.py && echo "5-tuple unpack: ok"</automated>
    <automated>grep -Eq "system_view\.set_wan_ip\(wan_ip\)" weather_view.py && echo "forward-to-system call: ok"</automated>
    <automated>grep -Eq "clock_view\.set_tz_offset\(tz_offset\)" weather_view.py && echo "existing clock forward preserved: ok"</automated>
    <automated>grep -c "temp, code, is_day, tz_offset = weather.current()" weather_view.py | grep -Eq "^0$" && echo "no stale 4-tuple unpack: ok"</automated>
    <automated>grep -c "spinner" weather_view.py | grep -Eq "^0$" && echo "spinner-free (quick 260718 preserved): ok"</automated>
  </verify>
  <acceptance_criteria>
    - `weather_view.py` parses.
    - Exactly one `import system_view` line at the top.
    - `weather.current()` result unpacked as 5-tuple.
    - `system_view.set_wan_ip(wan_ip)` called AFTER `clock_view.set_tz_offset(tz_offset)` on the successful path.
    - No stale 4-tuple unpack.
    - No spinner references (quick 260718 preserved).
  </acceptance_criteria>
  <done>weather_view.py forwards WAN IP to system_view on every successful weather fetch, alongside the existing tz_offset forward to clock_view.</done>
</task>

<task type="auto">
  <name>Task 3: Full replacement of system_view.py (state + setter + bar helpers + render)</name>
  <files>system_view.py</files>
  <read_first>
    - system_view.py (current 2-line stub)
    - clock_view.py (reference: set_tz_offset pattern, cache initialization, dual-gate render)
    - .planning/phases/04-system-view/04-CONTEXT.md (D-41..D-45 and D-43-bis — all binding)
    - .planning/codebase/CONVENTIONS.md (snake_case, `_`-prefix privates, no docstrings, no type hints, `.format()`)
  </read_first>
  <action>
    REPLACE `system_view.py` entirely with the target implementation shown in `<interfaces>` above. Full file rewrite via a single Write operation (5 sites affected — module state, 2 helpers, setter, render — small-edit approach risks missing one).

    Do NOT include:
    - Any docstring
    - Any type hints
    - Any f-string (all format via `.format()` or string concatenation with `+`)
    - Any `_center_text` helper (system view is left-aligned)
    - Any file I/O (WAN IP is RAM-only per D-43-bis)
    - Any WiFi write operations (`connect`, `disconnect`, `active(True)`, etc. — D-45 read-only)
    - Any `should_tick` or `should_sync` predicate (D-41 view-switch-only)

    Include:
    - `import network`
    - `import text_render`
    - `from sh1107 import WIDTH`
    - Module state `_cached_wan_ip = None`
    - Two private helpers: `_draw_bars(oled, x, y, level)` and `_rssi_to_bars(rssi)`
    - Public setter `set_wan_ip(ip)` with None guard AND same-value idempotent guard
    - `render(oled)` with online/offline branch on `wlan.isconnected()`; IP field uses dual gate (`connected AND _cached_wan_ip is not None`); signal bars use try/except around `wlan.status("rssi")` (defensive against firmware variance)

    Preserve conventions:
    - `oled.fill(0)` as the first line of `render`
    - String concatenation with `+` for SSID and IP display lines (matches the "no f-strings" convention; equivalent to `.format()` but simpler for two-string cases)
    - SSID truncation to `min(15, WIDTH // 8)` chars with NO ellipsis
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('system_view.py').read()); print('system_view.py: syntax ok')"</automated>
    <automated>grep -Eq "^import network$" system_view.py && echo "network import: ok"</automated>
    <automated>grep -Eq "^import text_render$" system_view.py && echo "text_render import: ok"</automated>
    <automated>grep -Eq "^from sh1107 import WIDTH$" system_view.py && echo "WIDTH import: ok"</automated>
    <automated>grep -Eq "^_cached_wan_ip = None" system_view.py && echo "cache state: ok"</automated>
    <automated>grep -Eq "^def _draw_bars\(oled, x, y, level\):" system_view.py && echo "_draw_bars sig: ok"</automated>
    <automated>grep -Eq "^def _rssi_to_bars\(rssi\):" system_view.py && echo "_rssi_to_bars sig: ok"</automated>
    <automated>grep -Eq "^def set_wan_ip\(ip\):" system_view.py && echo "set_wan_ip sig: ok"</automated>
    <automated>grep -Eq "if ip is None:" system_view.py && echo "None guard: ok"</automated>
    <automated>grep -Eq "if ip == _cached_wan_ip:" system_view.py && echo "same-value guard: ok"</automated>
    <automated>grep -Eq "^def render\(oled\):" system_view.py && echo "render sig: ok"</automated>
    <automated>grep -Eq "wlan\.isconnected\(\)" system_view.py && echo "isconnected read: ok"</automated>
    <automated>grep -Eq 'wlan\.config\("essid"\)' system_view.py && echo "essid read: ok"</automated>
    <automated>grep -Eq 'wlan\.status\("rssi"\)' system_view.py && echo "rssi read: ok"</automated>
    <automated>grep -Eq "if connected and _cached_wan_ip is not None:" system_view.py && echo "IP dual-gate: ok"</automated>
    <automated>grep -Eq 'rssi >= -55' system_view.py && grep -Eq 'rssi >= -65' system_view.py && grep -Eq 'rssi >= -75' system_view.py && echo "RSSI thresholds: ok"</automated>
    <automated>grep -c "def " system_view.py | grep -Eq "^4$" && echo "exactly 4 defs (_draw_bars, _rssi_to_bars, set_wan_ip, render): ok"</automated>
    <automated>grep -c '"""' system_view.py | grep -Eq "^0$" && echo "no docstrings: ok"</automated>
    <automated>grep -Ec "^import|^from " system_view.py | grep -Eq "^3$" && echo "exactly 3 imports: ok"</automated>
    <automated>grep -c "wlan\.connect\|wlan\.disconnect\|wlan\.active" system_view.py | grep -Eq "^0$" && echo "no WiFi write ops (D-45 read-only): ok"</automated>
    <automated>grep -c "should_tick\|should_sync" system_view.py | grep -Eq "^0$" && echo "no predicates (D-41 view-switch-only): ok"</automated>
    <automated>grep -c "open(" system_view.py | grep -Eq "^0$" && echo "no file I/O (D-43-bis RAM-only): ok"</automated>
  </verify>
  <acceptance_criteria>
    - `system_view.py` parses.
    - Exactly 3 imports: `network`, `text_render`, `WIDTH from sh1107`.
    - Module state `_cached_wan_ip = None` declared once.
    - Four function definitions: `_draw_bars`, `_rssi_to_bars`, `set_wan_ip`, `render`.
    - `set_wan_ip` has both None guard and same-value idempotent guard.
    - `render` uses `wlan.isconnected()` as the top-level branch condition.
    - `render` uses dual-gate (`connected AND _cached_wan_ip is not None`) for the IP field.
    - `render` reads `wlan.status("rssi")` inside try/except with fallback to 0 bars.
    - RSSI thresholds match D-42: -55/-65/-75.
    - Zero docstrings, zero WiFi write ops, zero predicates, zero file I/O.
  </acceptance_criteria>
  <done>system_view.py implements the full diagnostics view per D-41..D-45; passive read-only integration with network.WLAN; view-switch-only refresh via main.py's existing carousel branch.</done>
</task>

<task type="auto">
  <name>Task 4: Anti-diff verification on unchanged files</name>
  <files>(none — verify-only)</files>
  <action>
    Run anti-diff verification against files this plan MUST NOT modify. No file edits.
  </action>
  <verify>
    <automated>git diff --exit-code sh1107.py wifi.py icons.py text_render.py clock_view.py main.py && echo "unchanged files: ok"</automated>
    <automated>python3 -c "import ast; ast.parse(open('weather.py').read()); ast.parse(open('weather_view.py').read()); ast.parse(open('system_view.py').read()); print('all 3 modified files: syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Zero diff on sh1107.py, wifi.py, icons.py, text_render.py, clock_view.py, main.py.
    - All 3 modified files parse as valid Python 3.
  </acceptance_criteria>
  <done>Anti-diff invariants hold; every touched file still parses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: On-device verification of System view</name>
  <what-built>
    - `weather.py` returns 5-tuple with wan_ip; URL requests offset+query fields.
    - `weather_view.refresh` forwards WAN IP to `system_view.set_wan_ip` on every successful fetch.
    - `system_view.render` displays SSID + WAN IP + drawn signal bars when online; uniform `--` when offline.
    - `main.py` unchanged — existing carousel branch dispatches to `system_view.render` on view-switch to index 2.
  </what-built>
  <how-to-verify>
    1. Copy all 3 changed source files to the Pico:
       `mpremote cp weather.py weather_view.py system_view.py :`
       Then `mpremote run main.py` (or reset the device).

    2. **Cold-boot behavior (WAN IP cache empty):**
       - Weather view boots first as usual.
       - Press KEY1 twice to navigate to the System view (Weather → Clock → System).
       - Expected immediate state (before first weather fetch completes):
         - Line 1: `SSID: <your-network-name>` (or truncated to 15 chars)
         - Line 2: `IP: --` (WAN cache not populated yet)
         - Line 3: `Signal ` followed by 4 bars, filled per your actual RSSI
       - Wait for the first weather fetch to complete (up to 10 min normal cadence, or watch the Weather view — icon + temp appearing signals success).
       - Navigate back to System (KEY0 twice). Now:
         - Line 2: `IP: <your.public.ip>` (e.g. `IP: 203.0.113.5`)
         - Other lines unchanged.

    3. **SSID truncation:**
       - Confirm the SSID display shows the connected AP's name. If your SSID is longer than ~15 chars, confirm it truncates without ellipsis.

    4. **Signal bars:**
       - Compare the bar count against actual signal strength. Move the device closer to / farther from the AP:
         - Very close (< 3 m, line of sight) → typically 4 bars (`>= -55 dBm`)
         - Same room / one wall → typically 3 bars
         - Across the house / multiple walls → 1-2 bars
       - Confirm the bars visibly change as you move. (May take a view-switch to refresh — press KEY0 then KEY1 to force a render.)

    5. **Offline behavior (SYSTEM-04):**
       - Turn WiFi off (unplug router OR set `secrets.py` to a wrong password + reboot).
       - Boot. Weather view shows "no wifi".
       - Press KEY1 twice to navigate to System.
       - Expected state:
         - Line 1: `SSID: --`
         - Line 2: `IP: --`
         - Line 3: `Signal ` followed by 4 hollow bars
       - All three fields uniform `--` per D-44.

    6. **Recovery path (WiFi returns after failure):**
       - While in the offline state, restore WiFi.
       - Wait for the next scheduled weather refresh (~60 s in retry mode, per WEATHER-09).
       - Navigate back to System (or press KEY0 then KEY1 to force re-render).
       - Now all three fields should show valid values again.

    7. **View-switch responsiveness:**
       - Confirm no visible lag when switching to System. `network.WLAN` reads are RAM-only, should be < 10 ms.
       - Confirm Weather and Clock views still work normally (no regression from the weather.py 5-tuple change or the weather_view.py new import).

    8. **Read-only verification (D-45):**
       - Structurally: `grep -c "wlan\.connect\|wlan\.disconnect\|wlan\.active" system_view.py` should be `0`. Already checked by Task 3's automated verify, but worth eyeballing the file too.
  </how-to-verify>
  <resume-signal>Type "approved" if (1) System view shows SSID + WAN IP + drawn signal bars matching actual signal strength when online, (2) all three fields show `--` when offline, (3) the IP field correctly shows `--` briefly at cold-boot before the first weather fetch populates the cache, (4) recovery from an offline period repopulates all three fields within one weather refresh cycle, (5) view-switch remains responsive (< one poll tick), and (6) Weather + Clock views continue to work without regression. Otherwise describe the observed behavior so a follow-up plan can adjust.</resume-signal>
</task>

</tasks>

<threat_model>

## New Threats Introduced by This Plan

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Denial of Service | `render(oled)` calling `wlan.status("rssi")` — could raise on unsupported firmware or hang if the driver is in a bad state | mitigate | Inline try/except around the `wlan.status("rssi")` read; falls back to 0 bars if the call raises. `isconnected()`, `config("essid")`, `ifconfig()` are stable across firmware versions; only `status("rssi")` is defensively wrapped. |
| T-04-01-02 | Tampering | Malicious ip-api response with a wrong `query` value | accept | Same posture as tz_offset. Impact bounded: displayed WAN IP is wrong, no state corruption. RAM-only cache means next boot re-fetches. |
| T-04-01-03 | Information Disclosure | WAN IP visible to onlookers via the panel | accept | This is a diagnostics view intentionally showing diagnostic info. If the operator is uncomfortable displaying the WAN IP publicly, they can navigate away from System view. No secrets are exposed (WAN IP alone is not sensitive; it's already broadcast to every server the device contacts). |
| T-04-01-04 | Elevation of Privilege | Circular import `weather_view → system_view → weather_view` | mitigate | Verified in `<interfaces>`: system_view imports only network, text_render, WIDTH from sh1107. No back-edge. |
| T-04-01-05 | Denial of Service | `system_view.render` blocking the poll loop | accept | All `wlan.*` reads are RAM-only, non-blocking, sub-millisecond. Total render time (including `text_render.text` calls and bar drawing) well under one poll tick (100 ms). |
| T-04-01-06 | Tampering | `wlan.config("essid")` returning bytes instead of str on some firmware | mitigate | Not currently defended against — the executor will observe and add a `.decode()` if bytes is returned. Documented in `<interfaces>` "Notes" so the executor is aware. Fallback: if the executor observes bytes on-device, this plan gains a Task-3.5 to add the decode; the anti-diff invariants stay intact. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages. Only MicroPython stdlib `network` module added to imports. |

Zero HIGH severity threats. `security_asvs_level: 1`, `security_block_on: high`.

</threat_model>

<verification>

Executor MUST confirm before Task 5 begins:

1. All 4 previous tasks' verify blocks pass (~30 automated assertions across weather.py, weather_view.py, system_view.py, and the anti-diff step).
2. Manual read-through of `system_view.py`: three imports, one state var, four function defs (helpers + setter + render), no docstrings, no type hints, no f-strings, no WiFi write ops, no predicates.
3. Manual read-through of `weather_view.py`: `import system_view` positioned between `clock_view` and `icons`; 5-tuple unpack; `system_view.set_wan_ip(wan_ip)` immediately after `clock_view.set_tz_offset(tz_offset)`.
4. `git diff --exit-code sh1107.py wifi.py icons.py text_render.py clock_view.py main.py secrets.py` — anti-diff on all non-target files (note: `main.py` is in this list — Phase 4 makes ZERO changes to main.py).
5. All 3 modified source files parse via ast.

Task 5 checkpoint is BLOCKING per `workflow.human_verify_mode: end-of-phase`. Six verify criteria must all be observably confirmed on-device.

</verification>

<success_criteria>

Plan complete when:
- SYSTEM-01..04 delivered end-to-end:
  - SYSTEM-01: connected SSID visible on System view (truncated at 15 chars).
  - SYSTEM-02: WAN IP visible when connected AND cache populated; `--` otherwise (interpretation per D-43-bis: WAN, not local).
  - SYSTEM-03: signal strength as 4 drawn bars, thresholded per D-42 (-55/-65/-75).
  - SYSTEM-04: uniform `--` for all fields when offline; view remains renderable and responsive.
- `weather.py` extended to 5-tuple; URL requests `offset` + `query` fields.
- `weather_view.py` unpacks 5-tuple, forwards WAN IP to `system_view.set_wan_ip` alongside the existing tz_offset forward.
- `system_view.py` implements full contract per `<interfaces>` — three imports, one cache field, four functions, no docstrings, no type hints, no f-strings, no WiFi write ops, no predicates, no file I/O.
- `main.py` unchanged.
- Zero diff on `sh1107.py`, `wifi.py`, `icons.py`, `text_render.py`, `clock_view.py`, `main.py`, `secrets.py`.

</success_criteria>

<output>
Create `.planning/phases/04-system-view/04-01-SUMMARY.md` when done.
</output>
