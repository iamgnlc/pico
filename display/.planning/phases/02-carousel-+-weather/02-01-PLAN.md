---
phase: 02-carousel-+-weather
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - weather_view.py
autonomous: false
requirements:
  - WEATHER-02
tags:
  - micropython
  - pico-w
  - sh1107
  - refactor
  - view-extraction

user_setup: []

must_haves:
  truths:
    - "Booting the Pico still shows the current temperature with the drawn degree ring, exactly as before Plan 01"
    - "The Weather rendering code lives in weather_view.py behind a render(oled) function"
    - "main.py imports weather_view and delegates the render to weather_view.render(oled)"
    - "sh1107.py is unchanged (byte-for-byte) — the four SH1107 gotchas remain untouched"
  artifacts:
    - path: "weather_view.py"
      provides: "Weather view module with render(oled) entry point"
      exports: ["render"]
      min_lines: 20
    - path: "main.py"
      provides: "Application entry — imports weather_view, delegates render, retains missing-secrets fallback and sleep loop"
      min_lines: 25
  key_links:
    - from: "main.py"
      to: "weather_view.render"
      via: "import weather_view; weather_view.render(oled)"
      pattern: "weather_view\\.render\\("
    - from: "weather_view.py"
      to: "wifi.connect + weather.current + icons.draw + text_render.text"
      via: "direct imports"
      pattern: "import (wifi|weather|icons|text_render)"
  anti_criteria:
    - "sh1107.py MUST NOT be modified in this plan (verifier will fail on any diff)"
    - "No new external dependencies added (no urequests_async, no third-party packages)"
    - "No f-strings introduced (project uses .format() per CONVENTIONS.md)"
    - "No type hints or docstrings introduced (MicroPython idiom)"
---

<objective>
Extract the Weather rendering from `main.py:_render()` into a new module `weather_view.py` exposing a single public function `render(oled)`. Zero behavior change: after Plan 01, booting the Pico shows the identical Weather screen (icon at (16,16), temperature centered at (88, HEIGHT//2), drawn degree ring) as it does today. `main.py` continues to own the missing-secrets fallback, the `_center_text` helper, and the `while True: _render(); time.sleep(REFRESH_SECONDS)` loop — those are replaced later in Plan 02.

Purpose: De-risk the Phase 2 refactor. Landing this plan first (a) proves the module boundary works on hardware before layering carousel + scheduler logic on top, and (b) gives Plan 02 a clean seam to hang the carousel dispatch off of. This is a preparatory vertical slice — the user-visible surface is unchanged, but the structural precondition for D-16/D-17/D-18 (three flat view files with `render(oled)`) is now in place.

Output:
- `weather_view.py` (new): contains the extracted render logic and its imports.
- `main.py` (modified): imports `weather_view`, calls `weather_view.render(oled)` in the sleep-loop body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-carousel-+-weather/02-CONTEXT.md
@.planning/phases/01-secure-foundation/01-CONTEXT.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@main.py
@weather.py
@wifi.py
@icons.py
@text_render.py
@sh1107.py

<interfaces>
<!-- Signatures used by weather_view.render. All exist in the codebase today. -->

From sh1107.py:
- class OLED(framebuf.FrameBuffer)
- WIDTH = 128
- HEIGHT = 64
- OLED inherits fill(color), ellipse(cx, cy, rx, ry, color, fill), text(...), etc. from framebuf.FrameBuffer
- OLED.show() — pushes the framebuffer to the panel

From wifi.py:
- connect(ssid, password, timeout=20) -> ip_str or None

From weather.py:
- current() -> (temp, code, is_day) or (None, None, None)

From icons.py:
- draw(fb, x, y, code, is_day) -> None

From text_render.py:
- text(fb, s, x, y, scale=1, color=1) -> None

Current main.py:_render(oled) body (lines 32-49) — this is the code being extracted:
- oled.fill(0)
- ip = wifi.connect(secrets.WIFI_SSID, secrets.WIFI_PASSWORD)
- if not ip: _center_text(oled, "no wifi", WIDTH // 2, HEIGHT // 2)
- else: temp, code, is_day = weather.current(); if temp is None: _center_text(oled, "no data", ...) else: icons.draw(oled, 16, 16, code, is_day); t = "{:.0f}".format(temp); _center_text(oled, t, 88, HEIGHT // 2, scale=2); w = 8 * len(t) * 2; cx = 88 + w // 2 + 5; cy = HEIGHT // 2 - 8 + 2; oled.ellipse(cx, cy, 2, 2, 1, False)
- oled.show()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create weather_view.py with the extracted render(oled) function</name>
  <files>weather_view.py</files>
  <read_first>
    - /Users/gnlc/Code/pico/display/main.py (canonical source of the render body being extracted; lines 32-49)
    - /Users/gnlc/Code/pico/display/CLAUDE.md (§"Non-obvious SH1107 gotchas" — this task does not touch sh1107.py, but any framebuf draws must respect the coordinate system origin-top-left, MONO_HMSB implicit assumption)
    - /Users/gnlc/Code/pico/display/.planning/codebase/CONVENTIONS.md (snake_case names, `.format()` not f-strings, no type hints, no docstrings — binding for the new module)
    - /Users/gnlc/Code/pico/display/.planning/phases/02-carousel-+-weather/02-CONTEXT.md (D-16 says three flat files at repo root, no views/ subdir; D-17 says `render(oled)` is the single public function)
    - /Users/gnlc/Code/pico/display/wifi.py, /Users/gnlc/Code/pico/display/weather.py, /Users/gnlc/Code/pico/display/icons.py, /Users/gnlc/Code/pico/display/text_render.py (existing modules whose signatures the new module calls — do not modify)
    - /Users/gnlc/Code/pico/display/sh1107.py (OLED class + WIDTH/HEIGHT constants imported from here — reference only, MUST NOT be modified)
  </read_first>
  <action>
    Create `/Users/gnlc/Code/pico/display/weather_view.py` at the repo root (flat namespace per D-16). Imports at the top in the same order convention used elsewhere in the codebase (see CONVENTIONS.md §"Import Organization"): stdlib-adjacent first, then local. Concretely: `from sh1107 import WIDTH, HEIGHT`, then `import wifi`, `import weather`, `import icons`, `import text_render`, then `import secrets` LAST (so an ImportError on secrets does not shadow errors in the other imports — matches the current main.py boot sequence discipline).

    Define a private helper `_center_text(oled, s, x_center, y_center, scale=1)` inside `weather_view.py` that is a byte-for-byte copy of the existing `_center_text` in `main.py:14-17`. Do NOT try to re-use the one in `main.py` — Plan 02 refactors `main.py` significantly and both files need their own copy. Keep the underscore prefix per CONVENTIONS.md.

    Define a public function `render(oled)` whose body is a direct extraction of `main.py:_render()` lines 33-49. Preserve every coordinate, every ellipse call, every format string, and the final `oled.show()` call. Concretely the extracted body is:

    - `oled.fill(0)`
    - `ip = wifi.connect(secrets.WIFI_SSID, secrets.WIFI_PASSWORD)`
    - `if not ip:` -> `_center_text(oled, "no wifi", WIDTH // 2, HEIGHT // 2)`
    - `else:` -> `temp, code, is_day = weather.current()` -> `if temp is None:` -> `_center_text(oled, "no data", WIDTH // 2, HEIGHT // 2)` -> `else:` -> `icons.draw(oled, 16, 16, code, is_day)`; `t = "{:.0f}".format(temp)`; `_center_text(oled, t, 88, HEIGHT // 2, scale=2)`; `w = 8 * len(t) * 2`; `cx = 88 + w // 2 + 5`; `cy = HEIGHT // 2 - 8 + 2`; `oled.ellipse(cx, cy, 2, 2, 1, False)`
    - `oled.show()`

    Do NOT change any coordinate values. Do NOT introduce type hints, docstrings, or f-strings. Keep `.format()` (per CONVENTIONS.md line 31 and D-06 Phase 1 lock).

    No caching, no state, no scheduler hooks — those are Plan 02/03 work. The single responsibility of this task is a functionally identical copy of the render body in its new home.
  </action>
  <acceptance_criteria>
    - File `/Users/gnlc/Code/pico/display/weather_view.py` exists.
    - `grep -c "^def render(oled)" /Users/gnlc/Code/pico/display/weather_view.py` returns 1.
    - `grep -c "^def _center_text(" /Users/gnlc/Code/pico/display/weather_view.py` returns 1.
    - `grep -q "oled.ellipse(cx, cy, 2, 2, 1, False)" /Users/gnlc/Code/pico/display/weather_view.py` (degree ring preserved).
    - `grep -q "icons.draw(oled, 16, 16, code, is_day)" /Users/gnlc/Code/pico/display/weather_view.py` (WEATHER-02 icon call preserved at the canonical (16, 16) coordinate).
    - `grep -q 'oled.show()' /Users/gnlc/Code/pico/display/weather_view.py` (final flush preserved).
    - `grep -c "import secrets" /Users/gnlc/Code/pico/display/weather_view.py` returns 1.
    - `grep -cE "f['\"]" /Users/gnlc/Code/pico/display/weather_view.py` returns 0 (no f-strings).
    - `grep -cE "def [a-z_]+\(.*: " /Users/gnlc/Code/pico/display/weather_view.py` returns 0 (no type hints on function params).
    - `python3 -c "import ast; ast.parse(open('/Users/gnlc/Code/pico/display/weather_view.py').read())"` exits 0 (syntactically valid Python — MicroPython syntax is a subset of CPython 3, this catches typos before flashing).
    - `git diff --exit-code /Users/gnlc/Code/pico/display/sh1107.py` returns 0 (sh1107.py unmodified).
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/gnlc/Code/pico/display/weather_view.py && grep -q "^def render(oled):" /Users/gnlc/Code/pico/display/weather_view.py && grep -q "^def _center_text(" /Users/gnlc/Code/pico/display/weather_view.py && grep -q "icons.draw(oled, 16, 16, code, is_day)" /Users/gnlc/Code/pico/display/weather_view.py && grep -q "oled.ellipse(cx, cy, 2, 2, 1, False)" /Users/gnlc/Code/pico/display/weather_view.py && python3 -c "import ast; ast.parse(open('/Users/gnlc/Code/pico/display/weather_view.py').read())" && git -C /Users/gnlc/Code/pico/display diff --exit-code sh1107.py</automated>
  </verify>
  <done>
    weather_view.py exists at the repo root, defines `render(oled)` and `_center_text(...)`, imports all required modules including `secrets`, preserves the exact icon and degree-ring coordinates from main.py, contains no f-strings or type hints, parses as valid Python 3, and sh1107.py has zero diff.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update main.py to delegate render to weather_view</name>
  <files>main.py</files>
  <read_first>
    - /Users/gnlc/Code/pico/display/main.py (current 57-line file — see what stays and what changes)
    - /Users/gnlc/Code/pico/display/weather_view.py (just created in Task 1 — the delegation target)
    - /Users/gnlc/Code/pico/display/CLAUDE.md (§ Project — flat-namespace convention, on-device only)
    - /Users/gnlc/Code/pico/display/.planning/phases/01-secure-foundation/01-CONTEXT.md (D-04/D-05 — missing-secrets fallback MUST stay in main.py; do NOT relocate it into weather_view.py)
    - /Users/gnlc/Code/pico/display/.planning/codebase/CONVENTIONS.md (import order)
  </read_first>
  <action>
    Modify `/Users/gnlc/Code/pico/display/main.py` with these exact changes and NO others in this plan:

    1. Add `import weather_view` to the imports block. Placement: after the existing local module imports (`import wifi`, `import weather`, `import icons`, `import text_render`) and before `import time`. This preserves the current stdlib-last ordering.

    2. Delete the `_render(oled)` function body (currently lines 32-49). The entire function goes away — its logic now lives in `weather_view.render()`.

    3. In the `__main__` block (currently lines 52-56), replace the call `_render(oled)` with `weather_view.render(oled)`. Everything else in the loop stays: the `OLED(rotate=ROTATE)` construction, the `while True:` shape, and the `time.sleep(REFRESH_SECONDS)` call.

    4. Keep the following untouched:
       - The user-config block (`REFRESH_SECONDS`, `ROTATE`) — these live in main.py per D-02 (Phase 1) and per the "user config at top of main.py" convention.
       - The `_center_text` helper — it's still used by the missing-secrets fallback (D-04 Phase 1). Do NOT delete this even though weather_view.py has its own copy now; the fallback needs it here.
       - The `try: import secrets / except ImportError:` fallback block (D-04, D-05 Phase 1) — MUST stay in main.py, unchanged.
       - The removal of the standalone `_render` reference in the `if __name__ == "__main__":` block — since the function is deleted, no dangling reference should remain.

    After the edit, main.py is expected to be roughly 40-45 lines (down from 57). Verify by reading the file back and confirming the `_render` function body is gone and `weather_view.render(oled)` is the sole render call.

    Do NOT introduce carousel logic, IRQ handlers, page dots, or a poll scheduler — those are Plan 02. Do NOT touch sh1107.py, wifi.py, weather.py, icons.py, or text_render.py.
  </action>
  <acceptance_criteria>
    - `grep -c "^import weather_view$" /Users/gnlc/Code/pico/display/main.py` returns 1.
    - `grep -q "weather_view.render(oled)" /Users/gnlc/Code/pico/display/main.py` (delegation call present).
    - `grep -cE "^def _render\(" /Users/gnlc/Code/pico/display/main.py` returns 0 (old _render function removed).
    - `grep -cE "^\s+_render\(oled\)" /Users/gnlc/Code/pico/display/main.py` returns 0 (no leftover local calls to the deleted function).
    - `grep -q "^def _center_text" /Users/gnlc/Code/pico/display/main.py` (missing-secrets helper preserved).
    - `grep -q "try:" /Users/gnlc/Code/pico/display/main.py` AND `grep -q "import secrets" /Users/gnlc/Code/pico/display/main.py` AND `grep -q "except ImportError:" /Users/gnlc/Code/pico/display/main.py` (D-04 fallback preserved).
    - `grep -q "REFRESH_SECONDS" /Users/gnlc/Code/pico/display/main.py` AND `grep -q "ROTATE" /Users/gnlc/Code/pico/display/main.py` (user config preserved).
    - `grep -q "time.sleep(REFRESH_SECONDS)" /Users/gnlc/Code/pico/display/main.py` (sleep loop still present — Plan 02 replaces it, not Plan 01).
    - `python3 -c "import ast; ast.parse(open('/Users/gnlc/Code/pico/display/main.py').read())"` exits 0.
    - `git -C /Users/gnlc/Code/pico/display diff --exit-code sh1107.py wifi.py weather.py icons.py text_render.py` returns 0 (unrelated files untouched).
    - **Operator on-device check (human-verify at phase end per config):** Flash `sh1107.py`, `weather_view.py`, `main.py` (plus existing wifi/weather/icons/text_render/secrets) to the Pico via `mpremote cp` and run `main.py`. Expected behavior: the OLED shows the current weather EXACTLY as it did before Plan 01 — condition icon at (16, 16), temperature digit(s) centered around x=88 with the small drawn degree ring to the upper-right of the last digit. If WiFi fails, screen shows "no wifi"; if weather API fails, screen shows "no data". No visible change from the pre-Plan-01 behavior. This checkpoint is deferred to the end-of-phase batch per `workflow.human_verify_mode=end-of-phase`.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "^import weather_view$" /Users/gnlc/Code/pico/display/main.py && grep -q "weather_view.render(oled)" /Users/gnlc/Code/pico/display/main.py && ! grep -qE "^def _render\(" /Users/gnlc/Code/pico/display/main.py && grep -q "^def _center_text" /Users/gnlc/Code/pico/display/main.py && grep -q "import secrets" /Users/gnlc/Code/pico/display/main.py && grep -q "except ImportError:" /Users/gnlc/Code/pico/display/main.py && grep -q "time.sleep(REFRESH_SECONDS)" /Users/gnlc/Code/pico/display/main.py && python3 -c "import ast; ast.parse(open('/Users/gnlc/Code/pico/display/main.py').read())" && git -C /Users/gnlc/Code/pico/display diff --exit-code sh1107.py wifi.py weather.py icons.py text_render.py</automated>
    <human-check>Flash to Pico and boot: OLED shows condition icon + temperature + degree ring identical to pre-Plan-01. Deferred to end-of-phase.</human-check>
  </verify>
  <done>
    main.py imports weather_view, delegates rendering to `weather_view.render(oled)`, has no residual `_render` function or calls, preserves the missing-secrets fallback and `_center_text` helper, keeps user config constants (`REFRESH_SECONDS`, `ROTATE`), keeps the `while True: ...; time.sleep(REFRESH_SECONDS)` loop, and touches no other source files. Syntax-checks green. sh1107.py and the other four unrelated modules have zero diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ip-api.com JSON -> weather.py parsing | Untrusted third-party JSON parsed via `.json()["lat"]` etc. Existing broad `except Exception` in weather.py catches; unchanged in this plan. |
| api.open-meteo.com JSON -> weather.py parsing | Same. Unchanged in this plan. |
| secrets.py (filesystem) -> weather_view.py | Local filesystem read of WiFi credentials via `import secrets`. Gitignored per Phase 1 D-01/D-12. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | weather_view.render() input JSON | accept | Broad `except Exception` in weather.py already catches KeyError/ValueError/network errors and returns (None, None, None); weather_view.render() checks `temp is None` and displays "no data". Existing behavior preserved by this refactor. `codebase/CONCERNS.md § API Response Parsing` flags this for future hardening — deferred (out of scope for a behavior-preserving refactor). |
| T-02-02 | Denial of Service | Boot-time `wifi.connect()` blocking | accept | Existing 20s timeout in wifi.connect(); no worse than pre-Plan-01. The scheduler in Plan 02/03 addresses non-blocking navigation during subsequent renders. |
| T-02-03 | Information Disclosure | secrets.py contents in on-device filesystem | accept | Same posture as Phase 1: gitignored, missing-secrets fallback halts safely (D-04/D-05). Not weakened by this refactor. |
| T-02-04 | Elevation of Privilege | new weather_view.py module import | mitigate | weather_view.py imports only stdlib-adjacent MicroPython modules and existing project files. No `exec`, no `eval`, no network eval. Manual code review during task execution. |

**Package legitimacy gate:** N/A — this plan installs zero packages. All imports resolve to MicroPython stdlib (`framebuf`, `network`, `time`, `machine`, `micropython`, `urequests`) or existing project files (`sh1107`, `wifi`, `weather`, `icons`, `text_render`, `secrets`). No npm/pip/cargo touched.
</threat_model>

<verification>
- All acceptance criteria in Task 1 and Task 2 pass.
- `git status` shows exactly two modified files (`main.py`) and one new file (`weather_view.py`); no other diff.
- End-of-phase on-device check (deferred): the OLED behavior is bit-for-bit identical to pre-Plan-01 — same icon, same temperature position, same degree ring, same error copy.
</verification>

<success_criteria>
- weather_view.py exists at the repo root and exports `render(oled)` with the extracted body.
- main.py imports weather_view and calls `weather_view.render(oled)` in place of the deleted `_render(oled)`.
- Missing-secrets fallback and `_center_text` helper remain in main.py (Phase 1 D-04 invariant preserved).
- sh1107.py, wifi.py, weather.py, icons.py, text_render.py have zero diff.
- Both files parse as valid Python 3 (MicroPython syntax subset).
- WEATHER-02 (condition icon rendering) is preserved — the `icons.draw(oled, 16, 16, code, is_day)` call moves intact into weather_view.py.
</success_criteria>

<output>
Create `/Users/gnlc/Code/pico/display/.planning/phases/02-carousel-+-weather/02-01-SUMMARY.md` on completion, listing:
- Files created: weather_view.py
- Files modified: main.py
- Line delta for main.py (should be roughly -15 to -17 lines)
- Confirmation that sh1107.py and the four unrelated modules were not touched
- Any implementation choices where planner discretion applied (e.g. import order tweak, comment additions)
</output>
